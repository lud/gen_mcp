---
title: Stateless server contract
status: new
created_on: 2026-05-29
parent: 1.feat.hard-fork-to-mcp-2026-07-28-stateless-core
---

# Stateless server contract

Rework `GenMCP.Suite` and the `GenMCP` behaviour so a server builds its state
per-request from `_meta` instead of from a handshake held in a session process.

## `GenMCP` behaviour (`lib/gen_mcp.ex`)

- **Drop** the session-oriented callbacks: `session_fetch/3`,
  `session_restore/3`, `session_delete/1`, `session_timeout/1` (lines 138-179)
  and their `@optional_callbacks`.
- `c:init/2` (line 114): the `session_id` argument no longer exists. Either
  drop it (`init/1`) or pass the request context. **Decision:** `init/1`
  receiving the validated server opts; per-request data arrives via the request
  + channel, not init. Document that init runs per request (cheap; opts are
  pre-validated at plug `init`).
- `handle_request/3` and `handle_notification/2` stay; remove session reply
  shapes that referenced restore.

## `GenMCP.Suite` (`lib/gen_mcp/suite.ex`)

- Remove the `{:__init__, ...}` two-phase init and the `InitializeRequest`
  handler (lines 122-160) -- there is no handshake.
- Refactor `initialize_from_req/4` (line 572) into
  `build_state(client_info, client_capabilities, opts)` driven by the request
  `_meta`, not an `InitializeRequest` struct. `capabilities/1` (line 704) is
  pure -- reuse.
- The `do_handle_request/3` clauses (lines 166-330) are reused as-is -- they
  already operate on `%State{}`.
- Add a **`server/discover`** clause:
  `do_handle_request(%MCP.V2607.DiscoverRequest{}, _, state)` returning a
  `DiscoverResult` built from the same `capabilities/1` helper that fed the old
  `InitializeResult`. This is the 2026 capability-discovery entry point.
- `%State{}` loses `session_id` and the `sc_*` (session controller) fields.
  Audit `State` (lines 74-100) and trim.

## Stateless runner (`lib/gen_mcp/stateless.ex`, new)

Thin glue between the transport (spec 003) and `Suite.handle_request/3`: given
pre-validated opts + the request `_meta` + the channel, build state via
`build_state/3` and dispatch in the calling process. No GenServer, no timeout.

## Tests

- Suite stateless unit tests: `tools/list`, `tools/call`, `resources/list`
  (pagination), `resources/read`, `prompts/list`, `prompts/get`,
  `server/discover` all work with state built from `_meta`.
- Confirm no `session_*` callbacks remain in the behaviour (compile-time:
  modules implementing `GenMCP` no longer need them).

## Acceptance criteria

- Full Suite feature parity over the stateless path.
- `server/discover` returns capabilities equivalent to the old
  `InitializeResult`.
- `GenMCP` behaviour has no session callbacks; `mix check` + dialyzer green.
